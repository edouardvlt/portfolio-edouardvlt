name: Deploy Lab Stack

on:
  push:
    branches:
      - lab  # Ce workflow ne se déclenchera QUE si tu pousses sur la branche lab

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Portainer Webhook
        run: |
          curl -X POST "${{ secrets.PORTAINER_WEBHOOK_URL_LAB }}"
